#!/bin/bash
if which pyclean >/dev/null 2>&1; then
    pyclean -p pritunl_client
else
    dpkg -L pritunl_client | grep \.py$ | while read file
    do
        rm -f "${file}"[co] >/dev/null
    done
fi
killall pritunl_client || true
